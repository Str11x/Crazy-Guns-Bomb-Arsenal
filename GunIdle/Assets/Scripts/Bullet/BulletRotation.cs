using System.Collections;
using UnityEngine;

public class BulletRotation : MonoBehaviour
{
    //[SerializeField] private int _fixedXAxisValue = 90;
    //private void Start() => StartCoroutine(Rotate());

    //private IEnumerator Rotate()
    //{
    //    while (Time.realtimeSinceStartup < 3)
    //    {
    //        transform.rotation = Quaternion.Euler(_fixedXAxisValue, 0, 0);
    //        yield return null;
    //    }
    //}
}